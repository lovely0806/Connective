version: '3.8'
services:
  connective-chat:
    build:
      context: .
      target: dev
    volumes:
      - ./src:/src
    command: npm run start:dev
    ports:
      - "3000:3000"
    env_file:
      - .env
      